using UnityEngine;

namespace RobEveryone.Core
{
    // Pure locatable marker, no logic -- where a rescued (or self-bailed)
    // player and their rescuer land, per gameplay-design.md's "outside the
    // back door of the police station."
    public class JailExitPoint : MonoBehaviour
    {
    }
}
