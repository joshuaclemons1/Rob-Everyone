<!--
  Landing page content — edit this file and push to update the
  "Already in the build" / "Coming next" lists on the public landing
  page. Nothing else needs to change: a GitHub Action
  (.github/workflows/site-data.yml) regenerates docs/site-data.js from
  this file (and pulls the current version/release date straight from
  GitHub Releases) on every push to it, so the page picks up the new
  copy within seconds — no HTML/CSS edits, no redeploy.

  Format: plain bullet list under each heading, one line each. Keep
  them short — they render as single-line list items on the page.
-->

## Already in the build

- Core heist loop & Jail & Bail
- Pawn shop & batch economy
- Homeowner & Police AI
- Day / Night rounds
- Steam proximity voice chat
- A real, rebindable Settings menu

## Coming next

- A launcher that updates itself
- Windows, macOS & Linux builds
- More houses to rob
- Music & sound pass
- A cleaner first-person view
